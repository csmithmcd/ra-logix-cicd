# Pinned dependencies for logix-designer-sdk 2.0.2 on Python 3.12 x64.
# The Rockwell wheel itself is installed separately from the verified local path.
cffi==1.17.1
clr-loader==0.2.7.post0
numpy==2.4.1
pycparser==2.22
pythonnet==3.0.5
